# OpenSearch Dashboards - Fixed configuration for OpenSearch 3.0.0 with SSL
replicaCount: 1

# Connect to OpenSearch via HTTPS (demo config enables SSL)
opensearchHosts: "https://opensearch-cluster-master:9200"

# Configuration for demo security setup with SSL
config:
  opensearch_dashboards.yml: |
    server.host: "0.0.0.0"
    opensearch.hosts: ["https://opensearch-cluster-master:9200"]
    opensearch.username: "admin"
    opensearch.password: "MyPassword123!"
    opensearch.ssl.verificationMode: none
    opensearch.ssl.certificateAuthorities: []
    opensearch_security.multitenancy.enabled: true
    opensearch_security.multitenancy.tenants.preferred: ["Private", "Global"]
    opensearch_security.readonly_mode.roles: ["kibana_read_only"]
    opensearch_security.cookie.secure: false

resources:
  requests:
    cpu: "100m"
    memory: "512M"

service:
  type: NodePort